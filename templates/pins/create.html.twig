<body>
    <h1>Create Pin</h1>

    {{ form_start(createForm) }}
            {{ form_widget(createForm) }}

                    <input type="submit" value="Create Pin">
    {{ form_end(createForm) }}

</body>